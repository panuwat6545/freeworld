// ค่าคงที่ทั้งหมดของระบบสังคม/ถิ่นฐาน (เฟส 4) แยกไว้ไฟล์เดียวเพื่อ tune ง่ายในอนาคต
export const SETTLEMENT_CONFIG = Object.freeze({
  // ระยะ (Chebyshev) ที่ถือว่าที่พัก 2 หลังอยู่ "ใกล้กัน" พอจะเป็นถิ่นฐานเดียวกัน (จัดกลุ่มแบบ transitive:
  // ถ้า A ใกล้ B และ B ใกล้ C ทั้ง 3 หลังจะถูกจัดเป็นถิ่นฐานเดียวกันแม้ A กับ C จะไกลกันเกิน radius ก็ตาม)
  CLUSTER_RADIUS: 6,

  // คำนวณกลุ่มถิ่นฐาน + ผู้นำใหม่ทุกๆกี่ tick แทนที่จะทำทุก tick (สิ้นเปลืองเกินจำเป็น เพราะที่พักไม่ได้
  // ถูกสร้างบ่อยขนาดนั้น)
  DETECTION_INTERVAL_TICKS: 10,

  // หน้าต่างเวลา (แบบ tumbling window) ที่ใช้นับว่าตัวละครไปพัก/สร้างใกล้ที่พักไหนบ่อยสุด เพื่อตัดสิน
  // homeSettlementId — ครบช่วงแล้วเริ่มนับใหม่ ป้องกันไม่ให้ประวัติเก่ามากๆ ถ่วงการย้ายถิ่นฐานจริง
  HOME_TRACKING_WINDOW_TICKS: 200,

  // เงื่อนไขการขยายเผ่าพันธุ์: ต้องอยู่ใกล้กัน (ระยะเดียวกับที่ socialize() ฟื้น social ได้ คือ isAdjacentOrSame)
  // และ social ของทั้งคู่ต้องสูงกว่าค่านี้ต่อเนื่องกันครบจำนวน tick ที่กำหนด
  //
  // ค่า TICKS_REQUIRED/COOLDOWN/MAX_POPULATION ด้านล่าง tune จากการรันจริงด้วย scripts/long-run.js
  // (30 ปี, ตัวละครตั้งต้น 9 ตัว): ค่าเริ่มต้นที่ลองก่อน (30 / 365 / 40) ทำให้ประชากรพุ่งชนเพดานภายใน
  // 5 ปีแรก จนโลก 40x40 (ที่ทรัพยากรถูก tune ไว้สำหรับ ~9 ตัวละคร) รองรับไม่ไหว เกิด flag "AI มีปัญหา"
  // (hunger/energy/social ค้างต่ำ) กับตัวละครจำนวนมากเพราะแย่งทรัพยากรกัน — ปรับเป็นค่าปัจจุบันแล้วประชากร
  // โตแบบค่อยเป็นค่อยไป (9->13->15 ตัว ใน 10 ปีแรก แล้วชนเพดานคงที่) ไม่มี flag ขึ้นเลยตลอด 30 ปี
  REPRODUCTION_PROXIMITY_RADIUS: 1,
  REPRODUCTION_SOCIAL_THRESHOLD: 80,
  REPRODUCTION_TICKS_REQUIRED: 300,

  // อายุขั้นต่ำ (tick นับจาก bornAtTick) ก่อนตัวละครจะเป็นคู่ขยายเผ่าพันธุ์ได้ — กันตัวละครที่เพิ่งเกิด
  // (needs เต็ม 100 อยู่ติดพ่อ/แม่พอดี) จับคู่กับพ่อ/แม่ตัวเองทันทีจนเกิดลูกวนไม่หยุดทั้งที่มี cooldown ต่อคู่แล้ว
  REPRODUCTION_MIN_AGE_TICKS: 100,

  // คูลดาวน์ต่อคู่หลังเกิดลูกไปแล้ว 1 คน (กันคู่เดิมปั๊มลูกรัวๆ ไม่หยุด)
  REPRODUCTION_COOLDOWN_TICKS: 1095,

  // เพดานจำนวนตัวละครทั้งหมดในโลก กันประชากรระเบิดจนควบคุมไม่ไหว/หน่วง performance และเกินความจุ
  // ทรัพยากรของโลก 40x40 (ดูหมายเหตุ tuning ด้านบน)
  MAX_POPULATION: 15,
});
