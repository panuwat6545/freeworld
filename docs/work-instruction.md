# Work Instruction: โปรเจกต์ Freeworld — เกมจำลองระบบนิเวศ

**Repo:** `freeworld` (GitHub)
**GCP Project:** `freeworld-508709`
**เก็บข้อมูลที่:** Google Drive ผ่าน Service Account (`GOOGLE_DRIVE_CREDENTIALS`)
**ผู้ดูแลโปรเจกต์:** Tonlyw
**เอกสารนี้คือ:** กฎการทำงานที่ Claude Code ต้องอ่านและทำตามทุกครั้งที่เริ่ม session ใหม่ในโปรเจกต์นี้

---

## 1. หลักการทำงาน (ห้ามฝ่าฝืน)

1. **ห้ามข้ามเฟส** — ต้องทำตามลำดับเฟสในข้อ 3 เท่านั้น ห้ามเริ่มเฟสถัดไปก่อนเฟสก่อนหน้าผ่านเกณฑ์ Definition of Done (ข้อ 4)
2. **ห้ามแก้ไฟล์ที่อยู่นอกขอบเขตของงานที่สั่ง** — แก้เฉพาะไฟล์ที่เกี่ยวข้องกับ task นั้นโดยตรง ถ้าพบบั๊กอื่นระหว่างทางให้รายงานแยก ไม่ต้องแก้เอง
3. **ทุก session ต้อง commit เป็นรอบเล็กๆ** พร้อมข้อความ commit ตามรูปแบบข้อ 5.2 ห้าม commit รวบยอดหลายฟีเจอร์ในครั้งเดียว
4. **ห้ามเขียน API key, credential, หรือ secret ลงในโค้ดหรือ commit ใดๆ** ให้อ่านผ่าน environment variable เท่านั้น (ดูข้อ 6)
5. **ก่อนปิดงานทุกเฟส ต้องอัปเดตส่วน "Changelog" ท้ายไฟล์นี้**

---

## 2. การแบ่งหน้าที่เครื่องมือ: Plugin / Skill / Tool

เพื่อไม่ให้ทำงานปนกันจนผิดเพี้ยน ให้ยึดหลักนี้ในการเลือกใช้:

| ประเภท | คืออะไร | ใช้เมื่อไหร่ | ตัวอย่างในโปรเจกต์นี้ |
|---|---|---|---|
| **Tool** | คำสั่งพื้นฐานที่เรียกตรง (bash, แก้ไฟล์, git, อ่าน/เขียนไฟล์) | งานทั่วไปทุกประเภท ใช้เป็นค่าเริ่มต้นเสมอ | สร้างไฟล์ `.js`, รัน `npm install`, `git commit` |
| **Skill** | ชุดคำแนะนำ/มาตรฐานสำเร็จรูปสำหรับงานเฉพาะด้าน ที่ทำให้ผลลัพธ์สม่ำเสมอทุกครั้ง | เมื่องานนั้นมีมาตรฐานที่ต้องทำซ้ำๆ และมีความเสี่ยงถ้าทำไม่ตรงแบบ | รีวิวโค้ดก่อน merge, ออกแบบ test strategy, เขียนเอกสาร README |
| **Plugin** | ชุดรวม skill + tool + command สำหรับทั้งโดเมน ติดตั้งเพิ่มเมื่อโปรเจกต์ต้องใช้ความสามารถเฉพาะทางต่อเนื่อง | เมื่อมีงานเฉพาะทางเกิดขึ้นซ้ำตลอดโปรเจกต์ (ไม่ใช่ครั้งเดียวจบ) | งาน data pipeline, งาน dashboard, งานที่ต้องเชื่อมบริการภายนอกซ้ำๆ |

**กฎการเลือก:** เริ่มจาก Tool เสมอ ถ้างานมีความเสี่ยงสูง (โครงสร้างระบบ, ตรรกะ AI ของตัวละคร, ความปลอดภัยของ credential) ให้ค้นหา Skill ที่เกี่ยวข้องก่อนลงมือ (เช่น `engineering:code-review`, `engineering:architecture`, `engineering:testing-strategy`) อย่าลงมือเขียนเองตามความรู้สึกถ้ามี Skill ที่ตรงงานอยู่แล้ว

---

## 3. ลำดับเฟสการพัฒนา

### เฟส 1 — โลกและทรัพยากร
- Grid โลกขนาด 40x40, ทรัพยากรธรรมชาติ (ไม้/น้ำ/แร่/อาหาร) งอกใหม่ตามเวลา
- ยังไม่มีตัวละครหรือ AI

### เฟส 2 — ตัวละครและระบบความคิด (Utility AI)
- ตัวละครมีค่าความต้องการ (หิว/พลังงาน/ที่อยู่/สังคม)
- ระบบเลือกพฤติกรรมตามความต้องการที่วิกฤตที่สุดโดยอัตโนมัติ (ไม่ใช้ LLM ต่อตัวละคร)

### เฟส 3 — ระบบสร้างสิ่งของ (Building/Crafting)
- ตัวละครเก็บทรัพยากรครบตามสูตร (blueprint) แล้วสร้างสิ่งก่อสร้างได้เอง

### เฟส 4 — ระบบสังคม/การขยายเผ่าพันธุ์ (ถ้าต้องการ)
- ปฏิสัมพันธ์ระหว่างตัวละคร, การขยายจำนวนประชากรในเกม
- **ถิ่นฐาน (Settlement):** กลุ่มที่พักที่อยู่ใกล้กันในระยะที่กำหนด เกิดขึ้นเองจากตำแหน่งการสร้างบ้าน
  ไม่ต้องมีใครสั่งตั้งใจ
- **สมาชิก:** ตัวละครที่บ้านของตัวเองอยู่ในถิ่นฐานนั้น
- **ผู้นำ:** กำหนดอัตโนมัติด้วยกฎง่ายๆ (เช่น สร้างที่พักมากสุด หรืออยู่นานสุดในถิ่นฐาน) เตรียมไว้ให้เฟส 8 ใช้
- **การขยายเผ่าพันธุ์:** ตัวละคร 2 ตัวที่ social สูงต่อกันนานพอเกิดตัวละครใหม่ในถิ่นฐานเดียวกัน

### เฟส 5 — บันทึกข้อมูลลง Google Drive
- บันทึกสถานะโลกเป็นไฟล์ JSON ตามรอบเวลาที่กำหนด (ดูข้อ 6)

### เฟส 6 — สกุลเงิน xcoin
- ตัวละครมี wallet เก็บ xcoin
- xcoin เกิดจากการค้า/ทำงาน
- มีอัตราเงินเฟ้อสุ่ม 2-10% ต่อปีเกม

### เฟส 7 — ระบบแลกเปลี่ยน
- รองรับทั้งซื้อขายด้วย xcoin และแลกของต่อของ (barter) ระหว่างตัวละคร

### เฟส 8 — กฎหมาย/การปกครอง
- ผู้นำถิ่นฐาน (จากเฟส 4) ออกกฎพื้นฐานได้ (เช่น จำกัดทรัพยากร)
- สมาชิกทำตามหรือฝ่าฝืนได้

### เฟส 9 — อาชีพที่เกิดเอง
- ตัวละครเลือกอาชีพตามทักษะ/ทรัพยากรในถิ่นฐาน
- ได้ xcoin จากการทำอาชีพ

### เฟส 10 — need พื้นฐานที่เปลี่ยนตามสภาพสังคม
- ปรับสูตร decay/target ของ need เดิม (หิว/พลังงาน/ที่อยู่/สังคม) ให้ขึ้นกับสภาพเศรษฐกิจ/กฎหมายของถิ่นฐาน
  ที่ตัวละครอยู่

---

## 4. Definition of Done ของแต่ละเฟส

เฟสจะถือว่า "เสร็จ" ก็ต่อเมื่อครบทุกข้อนี้:

- [ ] โค้ดรันได้จริงไม่มี error ใน console
- [ ] มีคำอธิบายสั้นๆ ในไฟล์ `README.md` ว่าเฟสนี้ทำอะไร ทดสอบยังไง
- [ ] ไม่มี credential หรือ API key หลงเหลือในโค้ด
- [ ] Commit แล้วขึ้น branch `main` (หรือ branch ย่อยที่ merge แล้ว)
- [ ] อัปเดต Changelog ท้ายไฟล์นี้

---

## 5. มาตรฐานโค้ดและโครงสร้างไฟล์

### 5.1 โครงสร้างโฟลเดอร์ในโปรเจกต์
```
/freeworld
  /src
    /world       -> โค้ดเกี่ยวกับแผนที่ ทรัพยากร
    /characters  -> โค้ดตัวละครและ Utility AI
    /building    -> ระบบสร้างสิ่งของ
    /social      -> ระบบสังคม (เฟส 4)
    /storage     -> โค้ดเชื่อม Google Drive
  /docs          -> เอกสารประกอบ (รวมไฟล์นี้)
  README.md
```

### 5.2 รูปแบบข้อความ commit
```
[เฟส-N] คำอธิบายสั้นๆ ว่าทำอะไร
```
ตัวอย่าง: `[เฟส-2] เพิ่มระบบคำนวณค่าความหิวและพฤติกรรมหาอาหาร`

### 5.3 การตั้งชื่อ
- ชื่อไฟล์/ฟังก์ชัน: ภาษาอังกฤษ, `camelCase` สำหรับ JS, `kebab-case` สำหรับชื่อไฟล์
- คอมเมนต์ในโค้ด: ภาษาไทยได้ เพื่อให้อ่านง่ายตอนกลับมาแก้

---

## 6. มาตรฐานการเชื่อมต่อ Google Drive

- อ่าน credential จาก environment variable ชื่อ `GOOGLE_DRIVE_CREDENTIALS` เท่านั้น ห้าม hardcode
- โครงสร้างโฟลเดอร์บน Drive:
```
/freeworld-ecosystem
  /snapshots        -> สถานะโลกทั้งหมด บันทึกทุกจบ 1 ปีในเกม
  /logs             -> เหตุการณ์สำคัญ (ตัวละครตาย, สร้างสิ่งของใหม่ ฯลฯ)
```
- ชื่อไฟล์: `snapshot_YYYY-MM-DD_HHmm.json`
- ทุกไฟล์ต้องมี field `timestamp`, `gameYear`, `worldState`, `characterCount` เป็นอย่างน้อย

---

## Changelog

| วันที่ | เฟส | รายละเอียด | ผู้ทำ |
|---|---|---|---|
| 2026-09-15 | เฟส 1 | สร้าง Grid โลกขนาด 40x40 พร้อมระบบทรัพยากรธรรมชาติ (ไม้/น้ำ/แร่/อาหาร) ที่งอกใหม่ตามเวลา | Claude Code |
| 2026-09-15 | เฟส 2 | เพิ่มตัวละคร (needs หิว/พลังงาน/ที่อยู่/สังคม), ระบบ decay ตามเวลา และ Utility AI ที่เลือกพฤติกรรมตาม need ที่เร่งด่วนที่สุดโดยอัตโนมัติ (หาอาหาร/พัก/เก็บไม้/เข้าสังคม) พร้อมระบบเดินแบบ greedy | Claude Code |
| 2026-09-15 | เฟส 3 | เพิ่มระบบสร้างสิ่งของ: blueprint "ที่พัก" (ใช้ไม้ 10), ตัวละครสร้างเองอัตโนมัติเมื่อเก็บไม้ครบสูตรผ่าน utility-ai, วางสิ่งก่อสร้างลงบน grid และหักทรัพยากรออกจาก inventory, พฤติกรรมพักฟื้นพลังงานเร็วขึ้นเมื่ออยู่ใกล้ที่พัก | Claude Code |
| 2026-09-15 | เฟส 5 | เพิ่มระบบบันทึก snapshot โลกเป็น JSON ขึ้น Google Drive ผ่าน service account (อ่าน credential จาก env `GOOGLE_DRIVE_CREDENTIALS`), อัปโหลดเข้า `/freeworld-ecosystem/snapshots` ชื่อไฟล์ `snapshot_YYYY-MM-DD_HHmm.json` สร้างโฟลเดอร์อัตโนมัติถ้ายังไม่มี, ผูก `SnapshotScheduler` ให้บันทึกอัตโนมัติทุกครั้งที่ครบ 1 ปีในเกม, มี error handling ไม่ให้เกมล่ม (ข้ามเฟส 4 ซึ่งเป็น optional ตามคำสั่งผู้ดูแลโปรเจกต์) — ทดสอบเชื่อมต่อจริงพบว่า service account ต้องใช้ Shared Drive หรือ domain-wide delegation เพราะไม่มี storage quota เป็นของตัวเอง (รอผู้ดูแลตั้งค่าฝั่ง Google Workspace) | Claude Code |
| 2026-09-16 | เฟส 5 | ปรับ `drive-client.js` ให้อ่าน field `type` ของ credential แล้วเลือก auth อัตโนมัติ: `authorized_user` ใช้ OAuth2Client + refresh_token (บัญชี Gmail ส่วนตัว ไม่มี Shared Drive) ยังรองรับ `service_account` เดิมไว้ (เผื่อกลับไปใช้ในอนาคต), เอา `supportsAllDrives` ออกจาก upload call ของไฟล์ snapshot, เพิ่ม unit test ครอบคลุมทั้ง 2 รูปแบบ credential — ทดสอบเชื่อมต่อจริงพบว่า environment variable ของ session นี้ยังเป็น credential แบบเดิม (`service_account` ที่ GCP project ถูกลบไปแล้ว) ยังไม่ใช่ `authorized_user` ใหม่ ต้องเปิด session ใหม่แล้วรัน `npm run demo:storage` อีกครั้งเพื่อยืนยันการเชื่อมต่อ Drive ส่วนตัวจริง | Claude Code |
| 2026-09-16 | เฟส-3-แก้ไข | แก้บั๊ก `buildShelter()` ใน `src/characters/behaviors.js` ที่ไม่เคยฟื้นค่า `character.needs.shelter` กลับหลังสร้างที่พักสำเร็จ (พบระหว่างรัน `scripts/long-run.js` จำลอง 30 ปี — shelter ค้างที่ 0 ตลอดกาล กลายเป็น need เร่งด่วนสุดทุก tick จนวนลูปสร้างไม่หยุด ไม่เคยไปกิน/พัก/เข้าสังคม) เพิ่ม `NEEDS_CONFIG.SHELTER_RESTORE_AMOUNT = 100` ให้ฟื้นเต็มทันทีที่สร้างสำเร็จ (เหตุผล: สร้างที่พักเป็น one-time event ต่างจาก need อื่นที่ฟื้นแบบค่อยเป็นค่อยไป) เพิ่ม unit test ครอบคลุมเคสนี้ใน `tests/building.test.js` รันยืนยันซ้ำด้วย `scripts/long-run.js`: flag "AI มีปัญหา" หายไปทุกช่วงเวลา (ปีที่ 5-30) ค่าเฉลี่ย need ทุกตัวขึ้นมาอยู่ที่ 80-93 (จากเดิม shelter/social ค้างที่ 0.0) และที่พักสะสมลดลงจาก 9,935 เหลือ 4,119 หลัง — ตัวเลขนี้ยังสูงอยู่เพราะต้นทุนสร้างถูกมากเทียบกับ decay rate ซึ่งเป็นเรื่อง tune สมดุลเกม ไม่ใช่บั๊ก จึงไม่ได้แก้ในรอบนี้ (นอกขอบเขตงาน) | Claude Code |
| 2026-09-16 | เฟส-3-แก้ไข | ปรับสมดุลเกมต่อจากรอบก่อน: เพิ่ม `BLUEPRINTS.shelter.cost.wood` จาก 10 เป็น 25 ใน `blueprints.js`, ปรับ `NEEDS_CONFIG.DECAY_RATE.shelter` ใน `needs-config.js` — ลองตามคำสั่งเริ่มต้น (ลด 40% จาก 0.6 เหลือ 0.36) แล้วยังได้ที่พักสะสม 2,210 หลัง/30 ปี ห่างจากเป้า (~3-5 หลัง/ตัวละคร/30ปี) มาก จึงทดลองลดเพิ่มเองหลายระดับพร้อมวัดผลจริงด้วย `scripts/long-run.js` ทุกครั้ง พบว่าลดต่ำกว่า 0.005 (เช่น 0.0035, 0.003) ทำให้ flag "AI มีปัญหา" กลับมาขึ้นจากปัญหาอื่นที่ไม่เกี่ยวกัน (ตัวละคร 1-2 ตัวติดวนลูปเพราะไม่ได้เดินผ่านทรัพยากรบ่อยเหมือนตอน shelter แข่งบ่อย — ปัญหาแยก ไม่แก้ในรอบนี้ รายงานแยกไว้) จึงเลือกค่าสุดท้าย `DECAY_RATE.shelter = 0.005` (ลด ~99% จากเดิม) ได้ที่พักสะสม 59 หลัง/30 ปี (~6.6 หลัง/ตัวละคร) ไม่มี flag ขึ้นเลย ใกล้เป้าหมายที่สุดในบรรดาค่าที่ปลอดภัย อัปเดต unit test ที่ผูกกับค่า config เดิม (`tests/building.test.js`, `tests/character.test.js`) ให้ใช้ค่าจาก config จริงแทนเลขตายตัว อธิบายเหตุผลการเลือกค่าและผลทดลองทั้งหมดไว้ใน README | Claude Code |
| 2026-09-16 | เฟส 4 | เพิ่มโมดูล `src/society/` ทั้งหมดตามนิยามเฟส 4 ที่อัปเดตใน work-instruction.md: `settlement.js`/`settlement-detector.js` จัดกลุ่มที่พักเป็นถิ่นฐานด้วย union-find ตามระยะ `CLUSTER_RADIUS` คำนวณใหม่เป็นระยะหรือทันทีที่มีที่พักใหม่ (กันบั๊ก home-tracker พลาดจังหวะที่เจอจริงตอนรัน `demo:society` — ถิ่นฐานมีที่พักแต่ไม่มีสมาชิกเพราะ interval ยังไม่ถึงรอบ), `home-tracker.js` อัปเดต `character.homeSettlementId`/`homeSettlementSinceTick` (field ใหม่บน `Character`) จากพฤติกรรม rest/build_shelter โดยไม่แก้ `behaviors.js`/`utility-ai.js` เลย, `leadership.js` เลือกผู้นำตามกฎ+tie-break 3 ชั้น, `reproduction.js` เกิดตัวละครใหม่เมื่อ 2 ตัวใกล้กัน+social สูงต่อเนื่องครบเงื่อนไข จำกัดด้วย cooldown ต่อคู่+เพดานประชากร+อายุขั้นต่ำ (field ใหม่ `bornAtTick` กันลูกจับคู่กับพ่อแม่ตัวเองทันทีหลังเกิด — บั๊กที่เจอจริงตอนเขียน unit test), `society-system.js` รวมทุกอย่างเป็นจุดเดียว เพิ่ม `tests/society.test.js` (15 test), `src/society/demo.js` (npm run demo:society) และปรับ `scripts/long-run.js` ให้รายงานจำนวนถิ่นฐาน/ประชากร/ผู้นำทุก 5 ปี — รันจริง 30 ปีพบว่าค่าเริ่มต้นของเงื่อนไขขยายเผ่าพันธุ์ทำให้ประชากรพุ่งชนเพดาน (40) ภายใน 5 ปีจนโลก 40x40 รองรับไม่ไหว เกิด flag "AI มีปัญหา" กับตัวละครจำนวนมาก จึงปรับ `REPRODUCTION_TICKS_REQUIRED`/`REPRODUCTION_COOLDOWN_TICKS`/`MAX_POPULATION` ใน `settlement-config.js` ให้ประชากรโตค่อยเป็นค่อยไป (9->13->15 ตัวใน 10 ปีแล้วคงที่) ไม่มี flag ขึ้นเลยตลอด 30 ปี และเห็นผู้นำเปลี่ยนมือจริงตามธรรมชาติ 2 ถิ่นฐาน | Claude Code |
| 2026-09-16 | เฟส 6 | เพิ่มโมดูล `src/economy/` ตามสเปกเฟส 6: `wallet.js` (createWallet/deposit/withdraw ป้องกันยอดติดลบ), `inflation.js` (`InflationTracker` สุ่มเงินเฟ้อ 2-10% ทุกปีเกม ผูกกับ tick counter เดียวกับ `saveSnapshot`/`SnapshotScheduler` เก็บเป็น `cumulativeIndex` ทบต้นแทนตัวคูณราคาสินค้า เพราะเฟส 7/9 ยังไม่มีสินค้า/บริการให้อ้างอิงราคาจริง), `basic-income.js` (`BasicIncomeGenerator` PLACEHOLDER จ่าย xcoin สุ่มเล็กน้อยทุกปีแทน "การค้า/ทำงาน" ที่ยังไม่มี รอเฟส 9 แทนที่), `settlement-economy.js` (รวม/เฉลี่ยยอด xcoin ต่อถิ่นฐานแบบคำนวณสด — เลือกใช้ xcoin สกุลเดียว/เงินเฟ้อชุดเดียวทั้งโลก ไม่แยกเศรษฐกิจต่อถิ่นฐาน เพราะจะชนกับกลไก merge ถิ่นฐานจากเฟส 4 และเงินเฟ้อควรเป็นปรากฏการณ์ระดับสกุลเงินไม่ใช่ระดับหมู่บ้าน), `economy-system.js` รวมทุกอย่างเป็นจุดเดียว เพิ่ม field `wallet` บน `Character` (ทุกตัวละครรวมถึงที่เกิดใหม่จากเฟส 4 เริ่มด้วย `STARTING_BALANCE` เท่ากันหมด) เพิ่ม `tests/economy.test.js` (16 test), `src/economy/demo.js` (npm run demo:economy) และปรับ `scripts/long-run.js` ให้รายงานยอด xcoin เฉลี่ยต่อถิ่นฐาน+ดัชนีราคาสะสมทุก 5 ปี — รันจริง 30 ปี ยอด xcoin ไม่เคยติดลบ (ยังไม่มีทางใช้จ่ายในเฟสนี้) ดัชนีราคาสะสมโตเป็น ~5.9 เท่า (~490% สะสม) ซึ่งตรงตามคณิตศาสตร์ทบต้นของอัตราเฉลี่ย ~6%/ปี ไม่ใช่ค่าที่ระเบิดผิดปกติ | Claude Code |
| 2026-09-16 | เฟส 7 | เพิ่มโมดูล `src/trade/` ตามสเปกเฟส 7: `goods-trade.js` (ซื้อขายไม้ส่วนเกินด้วย xcoin ราคาผูกกับ `cumulativeIndex` จากเฟส 6 — จำกัดสินค้าไว้แค่ไม้เพราะอาหารไม่มี inventory จริงในโค้ดเฟส 2), `service-trade.js` (จ้างเก็บไม้แทน/จ้างช่วยพัก resolve ทันที 1 tick), `payment.js` (ลอง xcoin ก่อนเสมอ ไม่พอค่อย barter ใช้ social แทนเงิน), `trade-eligibility.js` (`canTransact` เงื่อนไขร่วม: ระยะ, ไม่วิกฤต, และ hunger ต้องปลอดภัยพอ), `trade-system.js` รวมทุกอย่างเป็นจุดเดียว เพิ่ม `tests/trade.test.js` (15 test), `src/trade/demo.js` (npm run demo:trade) และปรับ `scripts/long-run.js` ให้รายงานจำนวนธุรกรรมแยกประเภทสะสมทุก 5 ปี — รันจริง 30 ปีรอบแรกพบ flag "AI มีปัญหา" ใหม่ วินิจฉัยแยกทีละกลไกพบว่าเฉพาะบริการ "ช่วยพัก" (energy_help) ที่ทำให้ตัวละครไม่ต้องพักจริงจนแย่งอาหารกันรุนแรงขึ้น ลองแก้ด้วย cooldown ต่อคู่ก่อนแต่กลับแย่ลง จึงตัดออกแล้วเพิ่มเงื่อนไข `HUNGER_SAFETY_THRESHOLD` (50) ใน `canTransact` แทน (เข้มกว่าเกณฑ์วิกฤตทั่วไปเฉพาะ hunger) แก้ปัญหาได้สมบูรณ์ ระหว่างวินิจฉัยพบด้วยว่า `scripts/long-run.js` ไม่ deterministic เพราะ `InflationTracker`/`BasicIncomeGenerator` จากเฟส 6 ใช้ `Math.random()` จริง จึงฉีด `createRng` (จาก `src/world/random.js` ที่ `World` ใช้อยู่แล้ว) เข้าไปแทนในสคริปต์นี้ให้ผลรันซ้ำได้เหมือนเดิมทุกครั้ง — ยืนยันผลสุดท้าย: ไม่มี flag ขึ้นเลยตลอด 30 ปี ธุรกรรมสะสม 106 ครั้ง (xcoin 88, barter 18) ประชากร/ถิ่นฐาน/เงินเฟ้อเป็นไปตามที่ tune ไว้ในเฟสก่อนหน้า | Claude Code |
| 2026-09-16 | เฟส 8 | เพิ่มโมดูล `src/governance/` แบบสถาปัตยกรรมปลั๊กอินตามสเปกเฟส 8: `law.js` (นิยาม shape กลางของกฎหมาย + `LAW_EVENTS` + `clampParams`), `law-registry.js` (`LAW_REGISTRY` — จุดเดียวที่ต้องแก้เพื่อเพิ่มกฎหมายใหม่ในอนาคต), `trade-tax-law.js`/`territorial-access-law.js` (กฎหมายตัวอย่าง 2 ข้อ), `settlement-lawset.js` (`SettlementLawsetRegistry` เก็บชุดกฎหมายต่อถิ่นฐานแยก registry ไม่ยัดใส่ `Settlement` class ของเฟส 4 — key ด้วย settlementId ทำให้อยู่รอดข้ามการเปลี่ยนผู้นำเองอัตโนมัติ ตรวจสิทธิ์ "ต้องเป็นผู้นำเท่านั้น" จาก `settlement.leaderId` ปัจจุบันทุกครั้ง), `governance-system.js` (`GovernanceSystem` orchestrator — ภาษีการค้าทำงานแบบเก็บย้อนหลังจาก event ที่ `TradeSystem.update()` คืนมา ไม่แก้ `src/trade/` เลย ส่วนสิทธิ์เขตแดนทำงานผ่านการ wrap เมธอด `world.harvestAt` ของอินสแตนซ์จากภายนอกตอน construct ไม่แก้ class `World`/`behaviors.js`/`utility-ai.js` เลย ต้องให้สคริปต์ที่เรียกบอก `setCurrentHarvester()` ก่อน `updateCharacter()` แต่ละตัวเพราะ `harvestAt` เดิมไม่มี parameter ระบุผู้เก็บ) เพิ่ม `tests/governance.test.js` (10 test รวม regression-safe test), `src/governance/demo.js` (npm run demo:governance) และปรับ `scripts/long-run.js` ให้รันครึ่งแรก (ปีที่ 0-15) ไม่มีกฎหมาย ครึ่งหลัง (ปีที่ 15-30) ให้ผู้นำทุกถิ่นฐานเปิดกฎหมายทั้ง 2 ข้อ เทียบผลก่อน-หลัง — รันจริง 30 ปี deterministic ไม่มี flag "AI มีปัญหา" เพิ่มขึ้นเลยทั้งก่อนและหลังเปิดกฎหมาย คลังถิ่นฐานจากภาษีการค้าโตขึ้นเห็นผลจริง (เช่นถิ่นฐาน #2: 32.5->103.4 xcoin ปีที่20-30) ส่วนสิทธิ์เขตแดน block/penalty สะสม=0 ครั้งเพราะ 3 ถิ่นฐานในการรันนี้อยู่ห่างกันมากไม่มีใครเดินเข้าเขตกันจริง (พิสูจน์แล้วว่ากลไกทำงานถูกต้องผ่าน demo/unit test ที่บังคับสถานการณ์ตรงๆ) | Claude Code |
| 2026-09-16 | เฟส 9 | เพิ่มโมดูล `src/professions/` ตามสเปกเฟส 9: `profession.js` (`PROFESSION_REGISTRY` 5 อาชีพ — 4 อาชีพผูกกับทรัพยากรเฟส 1 ผ่าน `trackedBehavior`, คนรับจ้างเป็นอาชีพบริการจากเฟส 7), `profession-assignment.js` (`ProfessionAssignmentTracker` กำหนด/เปลี่ยนอาชีพจากพฤติกรรมสะสมแบบ tumbling window เหมือนเฟส 4 คูณน้ำหนักความขาดแคลนทรัพยากร `getScarcityWeight` (1.0-2.0) มีเกณฑ์ขั้นต่ำ `MIN_WEIGHT_TO_ASSIGN` กันพฤติกรรมแค่ 1-2 ครั้งถูกนับเป็นอาชีพทันที ไม่แก้ `behaviors.js`/`utility-ai.js` เลย), `income-generation.js` (ลบ `BasicIncomeGenerator`/`basic-income.js` ของเฟส 6 ทิ้งทั้งหมด แทนที่ด้วย `IncomeSupportGenerator` — คนไม่มีอาชีพได้เต็มจำนวนเท่าเดิม คนมีอาชีพได้เศษเสี้ยว `EMPLOYED_STIPEND_FRACTION` ต่อไปด้วย และ `ProfessionIncomeTracker` สังเกตรายได้จริงจากธุรกรรมเฟส 7 แยกตามอาชีพ) เพิ่ม `tests/professions.test.js` (13 test), `src/professions/demo.js` (npm run demo:professions) และปรับ `scripts/long-run.js` ให้รายงานการกระจายอาชีพ+รายได้สะสมต่ออาชีพทุก 5 ปี — รันจริง 30 ปีรอบแรก (ตัดรายได้ของคนมีอาชีพเหลือศูนย์ตามตัวอักษรของสเปก) พบ flag "AI มีปัญหา" ใหม่ที่ปีที่ 25/30 วินิจฉัยแยกทีละกลไกเหมือนเฟส 7 พบว่าไม่ใช่โมดูลติดตามอาชีพ (พิสูจน์แล้วว่าเป็นแค่การสังเกต ไม่กระทบผลลัพธ์เลยถ้าไม่แตะรายได้) แต่เป็นเพราะยอด xcoin หมุนเวียนที่ลดฮวบไปเปลี่ยนสัดส่วนธุรกรรม xcoin/barter ของเฟส 7 มากพอจะสั่นคลอนค่า social ผ่าน `SOCIAL_BARTER_BONUS` แล้วไปกระทบจังหวะ Utility AI เดิมของเฟส 2 แบบไม่ตั้งใจ (ระบบอ่อนไหวสูงกับปริมาณเงินหมุนเวียนรวม) ทดสอบเป็นขั้นๆ ตั้งแต่ 0% ถึง 100% พบช่วง 55-70% ไม่เกิด flag เลย จึงเลือก 60% เป็นค่ากึ่งกลางที่ปลอดภัย ยืนยันผลสุดท้าย: ไม่มี flag ขึ้นเลยตลอด 30 ปี รายได้จริงของคนทำฟาร์มจากธุรกรรมโตต่อเนื่องถึง 2,868.6 xcoin ที่ปีที่ 30 มากกว่าเงินสวัสดิการหลายเท่าตามเจตนาของสเปก | Claude Code |
| 2026-09-16 | เฟส 10 | เพิ่มโมดูล `src/social-conditions/` ตามสเปกเฟส 10 (เฟสสุดท้ายตามแผนเดิม): `condition-index.js` คำนวณดัชนีสภาพสังคม 3 ตัวจากข้อมูลเดิมล้วนๆ ไม่สร้างข้อมูลใหม่ (ความฝืดเคือง = expectedBalance จากเงินเฟ้อเฟส 6 ÷ wallet เฉลี่ยจริง, ความเหลื่อมล้ำ = coefficient of variation ของ wallet ในถิ่นฐาน, ภาระกฎหมาย = อัตราภาษี+สิทธิ์เขตแดนที่เปิดอยู่จริงจากเฟส 8), `needs-condition-modifier.js` (`NeedsConditionModifier` ห่อ `character.decayNeeds` จากภายนอกทีละตัวเหมือนที่เฟส 8 ห่อ `world.harvestAt` ไม่แก้ `needs-config.js`/`character.js` เลย ทิศทาง: hunger←ฝืดเคือง, social←เหลื่อมล้ำ, shelter←ภาระกฎหมาย, energy←ค่าเฉลี่ยทั้ง 3 ดัชนี ใช้ dead zone รอบจุดปกติกัน modifier เบี่ยงเบนจากความผันผวนธรรมชาติตามสเปกข้อ 3) เพิ่ม `tests/social-conditions.test.js` (22 test ครอบคลุมทิศทางผลกระทบทุกทิศทาง+ขอบเขต clamp+regression-safe), `src/social-conditions/demo.js` (npm run demo:social-needs) และปรับ `scripts/long-run.js` ให้รายงานดัชนี+modifier เฉลี่ยทุก 5 ปี — รันจริง 30 ปีรอบแรก (sensitivity ตามสัญชาตญาณ 0.4/0.5/0.5) พบ flag "AI มีปัญหา" ใหม่ที่ปีที่ 20-30 วินิจฉัยแยกทีละดัชนีพบว่าเปิดดัชนีไหนดัชนีเดียวก็เกิด flag ได้เกือบทุกตัว (มีแต่ความเหลื่อมล้ำอย่างเดียวที่ไม่เกิด) เพราะ Utility AI เดิมของเฟส 2 อ่อนไหวสูงกับการปรับ decay rate ต่อเนื่องยาวนาน (เจอปรากฏการณ์เดียวกับตอน tune เฟส 9) และความฝืดเคืองโตไม่มีเพดานตามธรรมชาติของเศรษฐกิจ (เงินเฟ้อสะสมโตเร็วกว่ารายได้คนส่วนใหญ่) ทำให้ขยาย dead zone ยังไงก็ไม่พอ ทดลองลด sensitivity ทั้ง 3 ตัวพร้อมกันแบบกวาดค่าตั้งแต่ 1%-100% ของค่าตั้งต้น พบว่าความสัมพันธ์กับการเกิด flag ไม่เป็นเส้นตรง/ไม่ monotonic เลย (จุดปลอดภัยเป็นจุดเล็กๆ แทรกระหว่างจุดไม่ปลอดภัย ต่างจากเฟส 9 ที่เจอช่วงกว้างต่อเนื่อง) จึงเลือกจุดที่ปลอดภัยและยืนยันด้วยจุดข้างเคียง (4.8%/5% ของค่าตั้งต้น) ก่อนสรุปว่าไม่ใช่บังเอิญ ลด sensitivity ทั้ง 3 เหลือ 0.02/0.025/0.025 (ราว 5% ของค่าตั้งต้น) ยืนยันผลสุดท้าย: ไม่มี flag ขึ้นเลยตลอด 30 ปี ดัชนียังคงตอบสนองถูกทิศทางและอิ่มตัวชนขอบเขต MAX_DEVIATION ได้ปกติในสถานการณ์รุนแรงจริง (ดู demo:social-needs) อัปเดต README สรุปภาพรวมทั้งโปรเจกต์ (เฟส 1-10) เป็น pipeline เดียวปิดท้าย | Claude Code |
